<%- include('../partials/header') %>

    <h1 class="text-center"><span class="border-bottom pb-1">Create a campground</span></h1>
    <form action="/campgrounds/<%= campground._id %>?_method=PUT" method="POST" class="formContent">
        <div class="form-group">
            <input class="form-control" type="text" value="<%= campground.name %>" name="campground[name]" maxlength="30" required>
        </div>
        <div class="form-group">
            <input class="form-control" type="number" value="<%= campground.price %>" name="campground[price]" min="2000" max="8000" step="1" required>
        </div>
        <div class="form-group">
            <input class="form-control" type="text" value="<%= campground.image %>" name="campground[image]" required>
        </div>
        <div class="form-group">
            <textarea class="form-control" name="campground[description]" rows="8" required><%= campground.description %></textarea>
        </div>
        <div class="form-group">
            <button class="btn btn-lg btn-primary btn-block">Submit!</button>
        </div>
        <a href="/campgrounds/<%= campground._id %>">Go Back</a>
    </form>

<%- include('../partials/footer') %>